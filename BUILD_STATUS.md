# flowhub-client-ts — INCOMPLETE SCAFFOLD

**Do not treat this directory as complete.** Only `package.json` and `tsconfig.json` exist so far.

See `../CLAUDE_CODE_PROMPT.md` (in the workspace root) for the full build plan. Execute steps 1–2 from the workspace root prompt before starting on this repo, because the client's types and fixtures are derived from the populated docs repo.
